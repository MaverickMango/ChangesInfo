===
update-node
---
TextElement: ) across  [4757,4766]
replace ) across  by ) across
===
update-node
---
TextElement: <p>The notion of "environment" is optimization-specific, but it should  [4873,4944]
replace <p>The notion of "environment" is optimization-specific, but it should  by <p>The notion of "environment" is optimization-specific, but it should
===
update-node
---
TextElement: .  [5136,5138]
replace .  by .
===
update-node
---
TextElement: Returns true if the node is pure, that is it side effect free and does it  [7694,7768]
replace Returns true if the node is pure, that is it side effect free and does it  by Returns true if the node is pure, that is it side effect free and does it
===
update-node
---
TextElement: Returns true if the two nodes have the same control flow properties,  [7986,8055]
replace Returns true if the two nodes have the same control flow properties,  by Returns true if the two nodes have the same control flow properties,
===
update-node
---
TextElement: <p>Implementations of this class will each provide own their  [19303,19364]
replace <p>Implementations of this class will each provide own their  by <p>Implementations of this class will each provide own their
===
update-node
---
TextElement:  The node immediately before which cfgSource  [14936,14981]
replace  The node immediately before which cfgSource  by  The node immediately before which cfgSource
===
update-node
---
TextElement: That is, could any of the concrete storage locations (fields, variables,  [18292,18365]
replace That is, could any of the concrete storage locations (fields, variables,  by That is, could any of the concrete storage locations (fields, variables,
