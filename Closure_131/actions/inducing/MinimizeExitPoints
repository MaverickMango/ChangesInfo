===
update-node
---
TextElement: the end of a block and removes them by moving the if node's siblings,  [6628,6698]
replace the end of a block and removes them by moving the if node's siblings,  by the end of a block and removes them by moving the if node's siblings,
